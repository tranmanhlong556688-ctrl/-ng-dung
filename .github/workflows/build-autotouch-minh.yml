name: Build Auto Touch Minh APK

on:
  workflow_dispatch:
  push:
    branches: [agent/autotouch-minh-v1]
    paths:
      - 'AutoTouchMinh/**'
      - '.github/workflows/build-autotouch-minh.yml'
  pull_request:
    paths:
      - 'AutoTouchMinh/**'
      - '.github/workflows/build-autotouch-minh.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: AutoTouchMinh

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK
        id: gradle_build
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          gradle --no-daemon clean assembleDebug 2>&1 | tee build.log

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: AutoTouchMinh_build_log
          path: AutoTouchMinh/build.log
          if-no-files-found: error
          retention-days: 14

      - name: Stop when Gradle failed
        if: steps.gradle_build.outcome == 'failure'
        run: exit 1

      - name: Prepare deliverables
        run: |
          mkdir -p output
          cp app/build/outputs/apk/debug/app-debug.apk output/AutoTouchMinh-v1.0.0-debug.apk
          zip -qr output/AutoTouchMinh-v1.0.0-source.zip . -x 'app/build/*' '.gradle/*' 'output/*' 'build.log'

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: AutoTouchMinh_v1.0.0_APK
          path: AutoTouchMinh/output/AutoTouchMinh-v1.0.0-debug.apk
          if-no-files-found: error
          retention-days: 30

      - name: Upload source
        uses: actions/upload-artifact@v4
        with:
          name: AutoTouchMinh_v1.0.0_Source
          path: AutoTouchMinh/output/AutoTouchMinh-v1.0.0-source.zip
          if-no-files-found: error
          retention-days: 30
